node_modules/
dist/
data/
*.log
# ralphex runtime (worktrees, progress) — not tracked
# plans, config, scripts, prompts — tracked
.ralphex/worktrees/
.ralphex/progress/
config.yaml.bak
config.yaml
crons.yaml

# Dolt database files (added by bd init)
.dolt/
dolt/
dolt-server.*
*.db
